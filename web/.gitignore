# See ../../.gitignore — keep secrets out of git
.env*.local
node_modules
.next
# web/out is the committed static Oil Desk export for oilchange serve (no npm on desktop).
# Rebuild with: npm run build:static
*.log
